## Build requirements

These tools are only required if DroidDeck is packaged inside a native Android app. They are not required to run the HTML simulator in a web browser.

- JDK 17
- Android SDK Platform 35
- Android SDK Build Tools 35.0.0
